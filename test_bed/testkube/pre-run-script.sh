
curl -X GET --location "https://https://xray.cloud.getxray.app/api/v2/export/cucumber?
                                                                  keys={{issueNumber}}" -o features.zip -H "Authorization: Bearer ${JWT}"